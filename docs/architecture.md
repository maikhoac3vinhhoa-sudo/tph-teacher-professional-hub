# TPH Architecture Notes

TPH starts as a modular React shell. Configuration, navigation, layout, routes, and reusable UI are separated so the same source can be customized for different teachers later.
